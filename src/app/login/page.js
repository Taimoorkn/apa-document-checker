import LoginForm from '@/components/auth/LoginForm';

export default function LoginPage() {
  return <LoginForm />;
}

export const metadata = {
  title: 'Sign In - APA Checker Pro',
  description: 'Sign in to your APA Checker Pro account to access your documents and continue checking APA compliance.',
};